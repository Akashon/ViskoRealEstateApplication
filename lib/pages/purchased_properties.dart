import 'package:flutter/material.dart';

class PurchasedPropertiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Purchased Properties")),
      body: const Center(
        child: Text("Purchased property details will appear here."),
      ),
    );
  }
}
